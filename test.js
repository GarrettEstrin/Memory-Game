function resetWrongAnswer(array) {

  array
  if (this.getAttribute('data-card') === 'a') {
    this.innerHTML = "<img src='images/a.png'>";
  }



  function setCardsToReset() {
      var resetCard1 = cardsInPlay[0];
      var resetCard2 = cardsInPlay[1];
  }

  function posScore(score) {
  return if score < 0 {
    var currentScore = 0;
  } else {
    currentScore = score;
  }
}
